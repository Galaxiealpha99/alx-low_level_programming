#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the alphabet in lowercase.
 *
 * Return: 0 on success
 */

int main(void)
{
	int debut = 0;

	for (debut = 0; debut < 10; debut++)
	{
		printf("%d",debut);
	}
	printf("\n");
	return (0);
}


